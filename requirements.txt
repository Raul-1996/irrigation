# Требуется Python 3.11+ (PEP 604 union types в коде, datetime.UTC).
# На WB (Debian 11, системный python3.9) ставится через uv — см. install_wb.sh.
Flask>=3.1.0
Pillow>=10.3.0
APScheduler==3.10.4
# SQLAlchemy is required by APScheduler's SQLAlchemyJobStore. Without it
# APScheduler silently falls back to MemoryJobStore and all one-shot jobs
# (zone_stop:N, master_close, ...) are lost on every `systemctl restart`.
# See audit-report PHYS-2 / MASTER-H10.
SQLAlchemy>=2.0,<3.0
# SQLAlchemy publishes greenlet behind architecture markers.  A lock compiled
# on Apple Silicon (arm64) otherwise omits the package required by Linux
# x86_64/aarch64, including GitHub CI and the Wirenboard target.
greenlet>=3.2,<4.0
Flask-WTF>=1.2.1
python-dotenv>=1.0.1
paho-mqtt==2.1.0
Flask-Sock>=0.7.0
hypercorn>=0.14.4
aiogram>=3.8.0
pycryptodome>=3.21.0
requests>=2.28.0
# Wave 2 observability baseline
python-json-logger>=2.0,<3.0       # F1: structured JSON log formatter
prometheus-client>=0.20,<1.0       # F2: /metrics endpoint + Counters/Gauges/Histograms
